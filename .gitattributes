# Force LF line endings for all text files in the repo, regardless of OS.
# Prevents CRLF being introduced on Windows (where core.autocrlf=true) and
# keeps Prettier's `endOfLine: lf` default consistent locally and in CI.
* text=auto eol=lf
